
### 🏛️ 支柱一：数据的底层逻辑（Data Foundations）

*这是你最强的部分，但 Python 侧可能有盲区。*

* **1.1 数据结构与运算**
* [ ] **Numpy 广播机制 (Broadcasting)**：理解为什么矩阵运算比 for 循环快（这是 Python 性能的核心）。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#numpy) (见 `🔢 NumPy` 章节)
    *   ❌ **缺失**: 没有说明为什么矩阵运算比 for 循环快
* [ ] **Pandas 内存优化**：`category` 类型、`int8/16/32` 的选择（处理大数据必备）。
    *   ❌ **缺失**: 需补充 `downcast`, `astype('category')` 等内存优化技巧。

* **1.2 统计学基础 (The "Why")**
* [ ] **分布 (Distributions)**：正态分布、长尾分布（Log-normal）。知道为什么要做 `np.log1p`？
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#0-log-transformation) (见 `纠偏大法` 章节)
    *   ❌ **缺失**: 需补充 "完整的分布可视化代码判断逻辑 > 需要纠偏" 的workflow代码
* [x] **假设检验**：p-value, 置信区间（A/B Test 的核心）。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#ab-test) (见 `假设检验 & A/B Test` 章节)
* [ ] **偏差与方差 (Bias-Variance Tradeoff)**：这是机器学习的核心哲学。过拟合（高方差）vs 欠拟合（高偏差）。
    *   ⚠️ **部分缺失**: 仅在面试准备中提及，缺乏详细原理解析。

### 🌲 支柱二：算法原理与选择（Algorithms）

*不需要推导公式，但要懂“它在这个场景下为什么比那个好”。*

* **2.1 线性模型 (基准线)**
* [ ] **Linear Regression**：理解权重（Weights）的含义，理解多重共线性（Multicollinearity）的危害。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#linear-regression) (见 `建模` 章节)
    *   ❌ **缺失**: 没有提及 weights
* [ ] **Logistic Regression**：理解 Sigmoid 函数，理解它输出的是概率。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#logistic-regression) (见 `建模` 章节)
    *   ❌ **缺失**: 没有提及 sigmoid
* [ ] **正则化 (Regularization)**：L1 (Lasso) vs L2 (Ridge)。知道 L1 可以做特征筛选。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#regularization) (见 `建模` 章节)
    *   ❌ **缺失**: 没有提及 正则化！

* **2.2 树模型 (表格数据王者)**
* [ ] **Decision Tree**：信息增益 (Information Gain)、熵 (Entropy)、基尼系数 (Gini)。
    *   ❌ **缺失**: 需补充决策树分裂原理。
* [ ] **Random Forest (Bagging)**：并行计算、Bootstrap 采样。为什么它不容易过拟合？
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#random-forest) (见 `建模` 章节)
    *   ❌ **缺失**: 没有提及 什么是 bagging，为什么不容易过拟合
* [ ] **Gradient Boosting (Boosting)**：串行计算、残差 (Residual) 拟合。XGBoost / LightGBM / CatBoost 的核心区别（面试必问）。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#sota-xgboost) (见 `SOTA 模型` 章节)
    *   ❌ **缺失**: 没有提及 什么是 boosting

* **2.3 无监督学习**
* [x] **K-Means**：聚类。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#k-means) (见 `聚类分析` 章节)
* [x] **PCA**：降维。理解主成分解释方差。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#pca) (见 `降维打击` 章节)

### ⚙️ 支柱三：特征工程体系 (Feature Engineering)

*这是决定模型上限的关键，也是 SQL 经验最能发挥的地方。*

* **3.1 数值处理**
* [ ] **归一化/标准化**：StandardScaler vs MinMaxScaler。树模型其实不需要，但线性模型必须做，为什么？
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#1-feature-scaling) (见 `缩放三剑客` 章节)
    *   ❌ **缺失**: 没有提及 树模型其实不需要，但线性模型必须做，为什么？
* [ ] **缺失值填补**：均值/中位数填补 vs 模型预测填补 vs 单独作为一类。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#cleaning) (见 `清洗 & 缺失值` 章节)
    *   ❌ **缺失**: 没有提及 我觉得这里说的不是简单的缺失值填补，应该是特征工程里例如diff(364)导致的nan值的处理……？
* [ ] **异常值处理**：Boxplot (IQR), Z-Score。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#outliers) (见 `可视化` 或 `DBSCAN` 章节)
    *   ❌ **缺失**: 没有提及 同上

* **3.2 类别处理**
* [x] **One-Hot Encoding**：优缺点（维度爆炸）。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#2-categorical) (见 `类别处理` 章节)
* [ ] **Label Encoding**：树模型可用。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#2-categorical) (见 `类别处理` 章节)
    *   还需要再深化下，目前掌握度不够
* [ ] **Target Encoding (Mean Encoding)**：高基数特征的神器，但要防泄露。
    *   ❌ **缺失**: 需补充 `category_encoders` 使用方法及防泄露技巧。

* **3.3 业务特征构造 (Feature Interaction)**
* [ ] **多项式特征**：。
    *   ❌ **缺失**: 需补充 `PolynomialFeatures`。
* [x] **统计特征**：Groupby Mean/Max/Std（你最擅长的 SQL 聚合）。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#aggregation) (见 `聚合 & 分组` 章节)

* **3.4 特征筛选**
* [x] **Filter**: 相关系数 (Correlation Matrix)。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#correlation-screening) (见 `共线性检查` 章节)
* [ ] **Wrapper**: 递归消除 (RFE)。
    *   ❌ **缺失**: 需补充 RFE 代码示例。
* [x] **Embedded**: 基于树模型的 Feature Importance。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#feature-importance) (见 `Feature Importance 驱动调优` 章节)

### 📉 支柱四：模型评估与优化 (Evaluation & Tuning)

*如何证明你的模型是靠谱的？*

* **4.1 评估指标 (Metrics)**
* [ ] **回归**：M[目标业务] (绝对误差), MSE (均方误差), RMSE (对大误差敏感), MAPE (百分比误差)。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#metrics-101) (见 `评估指标速查` 章节)
    *   ❌ **缺失**: 没有提及 RMSE为什么对大误差敏感？我现在只知道M[目标业务]/MSE
* [ ] **分类**：Accuracy (陷阱), Precision/Recall (权衡), F1-Score, AUC-ROC (排序能力), LogLoss (概率准确性)。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#metrics-101) (见 `评估指标速查` 章节)
    *   ❌ **缺失**: 没有提及 LogLoss 

* **4.2 验证策略 (Validation)**
* [ ] **K-Fold 交叉验证**：为什么比单次 Split 好？
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#optuna) (见 `自动调参` 代码中的 CV)
    *   ❌ **缺失**: 没有提及 K-Fold 交叉验证的原理，我也没明白这里的cv是什么意思
* [ ] **Stratified K-Fold**：分层抽样（解决类别不平衡）。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#advanced-splitting) (见 `数据切分高级技巧` 章节)
    *   ❌ **缺失**: 不明白和K-Fold的区别，目前知识库里也没有Stratified K-Fold这个名词
* [x] **Time Series Split**：时序数据的防泄露验证（重要！）。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#4-time-series-split) (见 `时序数据集拆分` 章节)

* **4.3 调参策略**
* [ ] **Grid Search** (网格搜索)：穷举，慢。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#optuna) (提及不推荐)
    *   ❌ **缺失**: 没有实际代码，我根本不了解
* [ ] **Random Search** (随机搜索)：快，效果不错。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#optuna) (提及)
    *   ❌ **缺失**: 没有实际代码，我根本不了解
* [x] **Bayesian Optimization (Optuna)**：智能搜索，当前最佳实践。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#optuna) (见 `自动调参: Optuna` 章节)

### 🛠️ 支柱五：工程化与落地 (ML Engineering)

*从 Notebook 到 Production，你之前问的 Pipeline 就在这里。*

* **5.1 数据流封装**
* [ ] **Sklearn Pipeline**：`fit` / `transform` / `predict` 的生命周期管理。
    *   ⚠️ **部分缺失**: 文中多次提及但未提供完整代码 Demo。
* [ ] **ColumnTransformer**：对不同列应用不同处理（数值列做缩放，类别列做编码）。
    *   ⚠️ **部分缺失**: 同上，缺乏实战代码。

* **5.2 模型解释性 (XAI)**
* [x] **SHAP Values**：归因分析（全局 vs 局部）。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#shap) (见 `SHAP 模型解释` 章节)
* [ ] **Permutation Importance**：置换重要性（模型无关的解释）。
    *   ❌ **缺失**: 需补充 `sklearn.inspection.permutation_importance`。

* **5.3 部署与监控**
* [ ] **Serialization**：`joblib` / `pickle`。
    *   [📘 知识库链接](../02_速查手册/knowledge_base_full.md#save-load) (见 `模型落地` 章节)
    *   ❌ **缺失**: 没有pickle 实际代码，我根本不了解
* [ ] **Concept Drift**：概念漂移（模型为什么会随时间失效）。
    *   ⚠️ **部分缺失**: 仅在搜索结果中提及，缺乏监控代码。

---

### 💡 建议的学习动作

1. **对照检查**：把上面这个大纲复制下来。
2. **标记状态**：
* ✅ **已掌握**：我能给别人讲清楚原理。
* ⚠️ **听说过**：大概知道是啥，但没手写过代码，或者不知道数学原理。
* ❌ **盲区**：完全没听过。

3. **填补盲区**：
* 对于 ⚠️ 和 ❌ 的点，去问你的 Agent 或者看 StatQuest。
* **例子**：如果你发现自己不懂 **Target Encoding** 的风险（过拟合），就专门去学这个点。